% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{ds_meta}
\alias{ds_meta}
\title{Metadata for one dataset}
\usage{
ds_meta(dataset)
}
\arguments{
\item{dataset}{a single dataset \code{id}, as listed by \code{\link[=ds_catalog]{ds_catalog()}}.}
}
\value{
A named list (the parsed metadata). Notable elements are \code{dim_order}
(the dataset's dimensions) and \code{dimensions} (each dimension's \code{levels},
keyed by code, with a \code{label}).
}
\description{
Returns the full metadata for a single dataset: its dimensions, the codes
(levels) available within each dimension, labels, source, license and date
range. Use this to discover which dimension codes to pass to \code{\link[=ds]{ds()}}.
}
\examples{
\donttest{
m <- ds_meta("ch_seco_gdp")
m$dim_order                       # "type", "structure", "seas_adj"
names(m$dimensions$type$levels)   # the codes you can pass as type = ...
}
}
