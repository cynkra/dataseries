## R CMD check results

0 errors | 0 warnings | 0 notes

## Notes

* Minor release. Adds a `lang` argument to `ds_catalog()` and `ds_search()`,
  and fixes three columns that were derived incorrectly from the API response
  (see NEWS.md).
* The package is relicensed from GPL-3 to MIT. The copyright holder is the
  sole author of the current code base.
* There are no reverse dependencies.
